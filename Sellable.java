public interface Sellable {
    
    // methods are not copleted
    // implements for interface 
    // no fields in a interface right?  
    
    double subtotal();
    double tax();
    double total();
    String receiptLine(); 
    
}